trigger:
- main

pool:
  name: Self-Hosted   # 👈 tuzya agent pool cha exact naam

steps:
- script: |
    echo "Hello from Self-Hosted Agent"
    echo "Agent Name: $AGENT_NAME"
    echo "Machine Name: $AGENT_MACHINENAME"
    uname -a
    whoami
  displayName: "Self Hosted Agent Connection Test"